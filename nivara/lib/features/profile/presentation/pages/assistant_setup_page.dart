import 'package:flutter/material.dart';

class AssistantSetupPage extends StatelessWidget {
  const AssistantSetupPage({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Assistant Setup')));
}
